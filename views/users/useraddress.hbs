{{>usernav}}

{{>profilehead}}

{{>profilenav}}


        <!-- Page Content  -->
<div id="content" class="p-4 p-md-5" ><br><br><br>
        <h2 class="mb-4">Address Info</h2>
        <a href="/useraddaddress" class="btn btn-outline-success">Add Address</a>&nbsp;&nbsp;&nbsp;
        <!-- Section: Design Block -->
<section>
  <div class="row d-flex justify-content-center">
    <div class="col-lg-8">
      <div class="form-check">
        {{#each address}}
            <input class="form-check-input" type="radio" name="address" id="f-option5" value="{{this._id}}" checked/>
              {{!-- <label class="form-check-label" for="f-option5">Paypal --}}
                <div class="card bg-light" style="width: 600px;">
                  <div class="card-body">
                    <h5 class="card-title "> <b>{{this.address.name}}</b> </h5>
                      <p class="card-text"> {{this.address.mobile}} <br> {{this.address.email}} <br>  {{this.address.address}} <br> {{this.address.pincode}} <br></p> <br>
                        <a href="/edit-address?address={{this._id}}" class="btn btn-outline-success">Edit</a>&nbsp;&nbsp;&nbsp;
                        <a href="/delete-address?address={{this._id}}" class="btn btn-outline-danger">Delete</a>
                  </div>
                 </div>
                                        </label>&nbsp;
                                        {{/each}}
                                    </div>
        </div>
      </div>
    </div>
  </div>
  </section>


{{profilejs}}

{{>userfooter}}

{{>userjs}}
